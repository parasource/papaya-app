import React from 'react';
import {View, StyleSheet} from 'react-native';

export const ArticlesCard = ({card}) => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})
